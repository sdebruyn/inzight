version: 2

models:
  - name: dim_dates
    description: A dimension table of dates
    columns:
      - name: day_type
        description: The type of day
        tests:
          - not_null
          - accepted_values:
              values:
                - weekend
                - weekday